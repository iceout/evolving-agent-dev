# Session Report: v0.3 Scope Note

## Goal

Create a minimal v0.3 scope note after Stage 0 `EXIT_WITH_CAVEATS` without finalizing a v0.3 process or changing Stage 0 evidence counts.

## Status

- Dogfood: yes.
- Stage 0 evidence status: post-Stage 0 v0.3 scoping; not a Stage 0 task.
- No Stage 0 counts changed.
- No v0.3 process was finalized.
- No skill, evaluation, process, policy, ADR, automation, tooling, or metrics changes were made.

## Changes

- Added `docs/v0.3-scope.md` as a short scope draft.
- Did not create `docs/process-v0.3.md`.
- Did not edit `docs/process-v0.2.md`, `skills/evolving-agent-process/SKILL.md`, `docs/evaluations/behavior-cases.md`, `docs/stage0-progress.md`, or `README.md`.

## Verification

Ran:

```bash
python3 - <<'PY'
from pathlib import Path
import re
import subprocess

scope = Path('docs/v0.3-scope.md')
report = Path('docs/session-reports/2026-06-17-v0.3-scope-note.md')
cases = Path('docs/evaluations/behavior-cases.md').read_text()

assert scope.exists()
assert report.exists()
assert not Path('docs/process-v0.3.md').exists()
text = scope.read_text()
assert 'Status: scope draft.' in text
assert 'Stage 0 counts are frozen' in text
assert 'thin executable behavior layer for Codex use' in text
assert 'Full agent architecture' in text
assert 'Automation or metrics' in text
assert 'Broad policy or ADR creation' in text
assert 'Promoting all evaluation candidates' in text
assert 'Turning every inbox observation into a rule' in text
for carryover in (
    'Sandbox/tooling caveat handling',
    'Cross-artifact stale-reference checks',
    'Planning impact analysis before implementation',
    'Final-response language adherence',
    'Same-file/sibling-module reuse scan',
):
    assert carryover in text, carryover
assert 'The Stage 0 tracker remains frozen' in text
assert 'External private evidence can guide scope but does not become internal accepted dogfood' in text
assert 'E008 requires a privacy-safe, judgeable, reproducible fixture' in text
assert '## Case E008:' not in cases

for path in (
    'README.md',
    'docs/process-v0.2.md',
    'skills/evolving-agent-process/SKILL.md',
    'docs/evaluations/behavior-cases.md',
    'docs/stage0-progress.md',
):
    subprocess.check_call(['git', 'diff', '--quiet', '--', path])

for dirname in ('docs/policies', 'docs/decisions', 'skills'):
    subprocess.check_call(['git', 'diff', '--quiet', '--', dirname])

changed = set(subprocess.check_output(['git', 'diff', '--name-only'], text=True).splitlines())
assert changed == set(), changed
status = set(subprocess.check_output(['git', 'status', '--short'], text=True).splitlines())
assert status == {
    '?? docs/session-reports/2026-06-17-v0.3-scope-note.md',
    '?? docs/v0.3-scope.md',
}, status

for path in (scope, report):
    for line_no, line in enumerate(path.read_text().splitlines(), 1):
        assert line.rstrip() == line, f'{path}:{line_no}: trailing whitespace'

print('v0.3 scope note ok')
PY

git diff --check
```

Result: both commands passed.

## Good

The scope note starts v0.3 conservatively by preserving Stage 0 boundaries while naming the first behavior-layer carryovers.

## Friction

No notable friction.

## Proposed Follow-up

- Policy note candidate: no.
- Casebook candidate: no.
- ADR needed? no.
- Evaluation candidate? no; E008 was not created.

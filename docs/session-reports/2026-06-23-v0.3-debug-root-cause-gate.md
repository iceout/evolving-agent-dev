# Session Report: v0.3 Debug Root-Cause Gate

## Goal

Add a lightweight v0.3 skill behavior check for high-risk debug or bad-case examples so agents establish root cause before local patches.

## Status

- Dogfood: yes.
- Stage 0 evidence status: post-Stage 0 v0.3 skill calibration; not a Stage 0 task.
- Stage 0 counts: no Stage 0 counts changed.
- v0.3 process status: no v0.3 process finalized.

## Changes

- Added `Debug / bad-case root-cause gate` under `v0.3 Behavior Checks` in `skills/evolving-agent-process/SKILL.md`.
- Kept the rule trigger-based and short: bad-case examples on high-risk paths require an evidence chain before local patches.
- Kept the existing Plan Artifact Rule unchanged.
- Did not create `docs/process-v0.3.md`, E008, policy, ADR, automation, tooling, metrics, or subagent framework.

## Verification

- Python assertion block passed: skill frontmatter parses, the debug root-cause gate exists under `v0.3 Behavior Checks`, required evidence-chain semantics are present, and the Plan Artifact Rule anchors remain unchanged.
- Python assertion block passed: `docs/process-v0.3.md` does not exist; `docs/stage0-progress.md` and `docs/evaluations/behavior-cases.md` are unchanged; no E008 exists.
- `git diff --check` passed; a temporary-index `git diff --cached --check` covering the new session report also passed.

## Good

The skill now distinguishes interim mitigation from final redesign without adding a broad debug workflow.

## Friction

No notable friction.

## Proposed Follow-up

- Policy note candidate: no.
- Casebook candidate: no.
- ADR needed? no.
- Evaluation candidate? no.

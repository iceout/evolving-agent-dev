# v0.3 Scope Note

Status: scope draft.

## Relationship to Stage 0

Stage 0 exited with caveats on 2026-06-17. Stage 0 counts are frozen; future dogfood and evidence should belong to v0.3 artifacts unless a Stage 0 correction or bookkeeping update is explicitly needed.

## Goal

v0.3 turns Stage 0 evidence into a thin executable behavior layer for Codex use. It should make the existing process easier to apply during real tasks, not become a full agent framework.

## Non-Goals

- Full agent architecture.
- Automation or metrics.
- Broad policy or ADR creation.
- Promoting all evaluation candidates.
- Turning every inbox observation into a rule.

## Carryovers

- Sandbox/tooling caveat handling.
- Cross-artifact stale-reference checks.
- Planning impact analysis before implementation.
- Final-response language adherence.
- Same-file/sibling-module reuse scan before adding helpers, serializers, coercion utilities, guards, or adapters.

## First Backlog

1. Scope and guardrails.
2. Skill checklist calibration, starting with planning impact scan and reuse scan.
3. Manual judge notes for E005-E007.
4. Local evidence ledger design note for privacy-preserving, local-only skill evidence; not runtime automation or metrics.

## Backlog Grooming Notes

### 2026-06-30 Real-Project Friction Distillation

| Pattern | Evidence | Current routing | Next action |
|---|---|---|---|
| Test-convenience IO seams can pollute production APIs | `docs/casebook/inbox.md` records test-friendly Mongo access friction around dependency-injection collection getters, thin wrappers, and `audit -> audit2` fallback semantics. | Inbox evidence; partly absorbed by `skills/coding-review-loop/SKILL.md` Focused Reuse Scan. | Continue observing. Possible future casebook or evaluation candidate only if repeated with a privacy-safe fixture and pass/fail rubric. |
| Review-fix patches can skip focused reuse scans for fakes and test doubles | `docs/casebook/inbox.md` records duplicated `_FakeMongoCursor` helpers after a subagent review finding; the fake modeled cursor chaining, `sort()`, `limit()`, and `limit(0)` semantics. | Inbox evidence; absorbed by `skills/coding-review-loop/SKILL.md` Focused Reuse Scan. | Watch future review-fix tasks. Candidate behavior is promising, but still needs a fixture before evaluation promotion. |
| Source-of-truth and fallback semantics need explicit review | Recent inbox entries and earlier external review-packet observations point to fallback/data-source choices becoming implicit contracts. | Existing v0.3 carryover plus `coding-review-loop` source-of-truth/fallback reminders. | Continue collecting fixture-quality evidence; do not promote to policy or formal evaluation yet. |

No new casebook, policy, ADR, or formal evaluation is needed from this grooming pass. Stage 0 counts stay frozen, and this does not create `docs/process-v0.3.md`.

### External Evidence Lineage

This is the current-state view for external patterns that need change attribution or later comparison. Distillation session reports preserve the immutable review record. Source IDs are privacy-safe central aliases, not target-project names or paths. See `docs/skill-design/local-evidence-ledger.md` for attribution and outcome rules.

#### CRL-X01 - Side-effect risk classification and packet timing

- Sources: `ext-crl-2026-07-a/C04`; external privacy-preserving evidence.
- Normalized pattern: user-impacting live writes, notifications, and side-effecting scheduled jobs or exports did not reliably trigger high-risk, packet-first handling before implementation.
- Original root-cause hypothesis: the skill listed action/export risk and required high-risk review before implementation, but did not operationally map these side effects to high-risk or state the first-edit timing. Confidence at distillation time: medium.
- Current assessment: the classification gap is confirmed at fixture level, but the causal link from explicit high-risk classification to packet timing remains inconclusive because the old snapshot already performed packet-first review on the live fixture. The external C04 ordering gap remains valid source evidence, but this pilot does not show that classification caused it.
- Repository rule state: `partial`; repository history contains the general packet rule from `580e744` and runnable acceptance from `a0c5022`, but not the explicit side-effect classification or first-edit timing.
- Source-task attribution: `loaded version unknown`; the case cannot establish an execution failure.
- Decision/change: accepted narrow clarification in `skills/coding-review-loop/SKILL.md` and `skills/coding-review-loop/references/review-packet-shape.md`, dated 2026-07-12 in `a90e366`. After the matched negative fixture exposed noun-based overtriggering, narrowed classification to changes in live side-effect capability or behavior in `30398fa`; unchanged copy/docs/formatting do not trigger this rule by themselves, and other risk surfaces remain independent.
- Later comparable evidence: no real-task comparable evidence. The initial 2026-07-12 conditional-efficacy pilot in `docs/session-reports/2026-07-12-crl-x01-conditional-efficacy-pilot.md` produced a fixture-level `sensitivity/specificity tradeoff`: classification changed, packet-first did not, and copy-only work overtriggered. The full current-versus-revised confirmation in `docs/session-reports/2026-07-12-crl-x01-capability-redesign-confirmation.md` used an independently validated equivalent fixture with identical prompt hashes; both live runs remained high-risk and packet-first, current copy overtriggered, and revised copy stayed lightweight. Confirmation result: fixture-level `supported`.
- Pre-redesign mechanism diagnosis: the `a90e366` rule keyed risk too broadly on notification-related task identity rather than whether the task changed live side-effect capability, targeting, delivery, default execution, or write behavior. The initial pilot observed a classification change, not a packet-first safety-behavior increment, plus a specificity regression on lightweight work; `30398fa` narrows that boundary.
- Next decision: the one redesign budget is spent and fixture confirmation supports the revised boundary. Stop synthetic iteration and observe known-version real tasks for both live capability changes and notification-adjacent copy/docs/formatting work.
- Outcome: `awaiting evidence`; confirmation supports the classification redesign at fixture level but does not establish real-task improvement or natural generalization.

#### CRL-X02 - Removal impact and runnable-entrypoint discovery

- Sources: `ext-crl-2026-07-a/C05`; external privacy-preserving evidence.
- Normalized pattern: removing a shared wrapper, adapter, or collection getter used a local reuse scan but missed repository-wide references and runnable maintenance entrypoints.
- Root-cause hypothesis: the existing scan was addition/change-oriented and scoped to current or sibling modules; removal work lacked a repository-wide symbol-impact and repository-native entrypoint-discovery gate. Confidence: high.
- Repository rule state: `absent`; the removal-specific repository-wide impact and entrypoint-discovery gate was not in the canonical skill before this change.
- Source-task attribution: `loaded version unknown`; the case does not prove that a newer rule was ignored.
- Decision/change: accepted generic removal/rename scan guidance in `skills/coding-review-loop/SKILL.md` and the packet's reuse rationale, dated 2026-07-12 in `a90e366`. The rule does not assume a single scripts-directory layout.
- Later comparable evidence: no real-task comparable evidence. The 2026-07-12 conditional-efficacy pilot in `docs/session-reports/2026-07-12-crl-x02-conditional-efficacy-pilot.md` compared known old/new skill snapshots on one blinded synthetic removal fixture. Both runs satisfied the frozen critical rubric and produced identical patches, so the pilot result was `no incremental signal`; its evidence level is fixture-conformant only.
- Outcome: `awaiting evidence`; the pilot showed that both snapshots could handle this fixture, but did not isolate incremental value from the new rule and is not real-task or natural post-change evidence.

#### CRL-X03 - Decision-field positive and blocker behavior

- Sources: `ext-crl-2026-07-a/C02`; external privacy-preserving evidence.
- Normalized pattern: a compact decision API could satisfy response-shape checks while misrepresenting positive actionability or blocker semantics.
- Root-cause hypothesis: semantic liveness required a reachable value and behavior test, but did not explicitly require a positive decision path plus representative blocker or negative paths. Confidence: medium.
- Repository rule state: `partial`; semantic liveness entered the repository in `f467a5f`, but did not explicitly require a positive decision path plus representative blocker or negative paths.
- Source-task attribution: `loaded version unknown`; whether the partial rule was available or executed is inconclusive.
- Decision/change: accepted a narrow behavior-test clarification in `skills/coding-review-loop/references/review-packet-shape.md`, dated 2026-07-12 in `a90e366`.
- Later comparable evidence: none.
- Outcome: `awaiting evidence`; the clarification has not yet been observed in a comparable task.

#### CRL-X04 - Test-double boundary fidelity

- Sources: existing inbox evidence and distillation commit `c953650`, with `ext-crl-2026-07-a/C01` and `C05` as possible supporting evidence.
- Normalized pattern: permissive middle-layer mocks or inaccurate fake call shapes can hide the production boundary contract.
- Root-cause hypothesis: the relevant packet section may have been unavailable, unloaded, or not converted into a completion check. Confidence: low because source-task versions are unknown.
- Repository rule state: `present` in the current canonical skill through the Test Double Boundary Fidelity guidance.
- Source-task attribution: `timeline inconclusive` for `C01`; `loaded version unknown` for `C05`.
- Decision/change: no new rule; current Test Double Boundary Fidelity guidance and watchlist item already express the reusable behavior.
- Later comparable evidence: the two external cases are behaviorally related but cannot be established as post-change executions.
- Outcome: `inconclusive`; do not label this repeated-after-change without confirmed version evidence.

#### CRL-X05 - Helper and public-contract boundaries

- Sources: existing focused-reuse and API-contract evidence, with `ext-crl-2026-07-a/C01` and `C03` as possible supporting evidence, plus `ext-crl-2026-08-b/C01` as external privacy-preserving supporting evidence. The latter is one real-task workflow and one evidence batch; its review, fix, and verification stages do not increase the evidence count.
- Normalized pattern: tiny helpers can obscure straight-line dataflow, while a public helper name can overstate the stability of a private row contract.
- Related supporting observation: `ext-crl-2026-08-b/C01` reports a cross-layer state-contract conflict in which a conditionally hidden client field was omitted from a diff while its persisted value remained. Omission did not by itself express a request to clear or resubmit that value, so the backend transition contract needed to distinguish a request value, an existing legal persisted value, a genuinely missing value, and an illegal value. Reuse of a persisted explicit source remains valid only when it satisfies the original business constraints; genuinely missing or illegal sources remain rejected. Behavior proof must cover the complete condition-removal, save, and condition-restoration sequence rather than only one request. This supports the existing `Schema/API/output contract`, `Source-of-truth/proxy/fallback`, `Required Behavior Tests`, and, when needed, `Reviewable Constraints` guidance; it does not create a generic hidden-field gate, backend fallback rule, state-machine contract, or feature-specific checklist.
- Root-cause hypothesis: these are related abstraction and contract-ownership symptoms, but the evidence does not establish a new common gate beyond focused reuse, public-interface risk, schema/API contract review, and behavior-level tests. Confidence: medium.
- Repository rule state: `present` in the current canonical skill through focused reuse, public-interface risk, schema/API contract review, and behavior-level tests.
- Source-task attribution: `loaded version unknown`; same-day ordering leaves the relevant rule timing inconclusive for the earlier `ext-crl-2026-07-a` cases, while `ext-crl-2026-08-b/C01` supplies no reliable loaded source or version. Neither establishes the historical loaded rule state.
- Decision/change: no change. Reuse existing Focused Reuse Scan, Reuse / Adapter Rationale, public-interface trigger, and schema/API/output contract guidance.
- Later comparable evidence: none with a confirmed post-change version.
- Outcome: `inconclusive`; continue as supporting evidence rather than a new independent issue or feature-specific checklist.

#### CRL-X06 - Multi-step mutation integrity

- Sources: `ext-crl-2026-07-b/C01` and `C02`, `ext-crl-2026-07-c/C01` and `C03`, `ext-crl-2026-07-d/C02`, `ext-crl-2026-07-e/C01`, `ext-crl-2026-08-a/C01`, plus `ext-crl-2026-08-c/C02`, `C04`, `C05`, `C07`-`C12`, and `C14`; external privacy-preserving evidence. The `ext-crl-2026-07-b` findings are one batch, and `ext-crl-2026-07-c/C01` and `C03` are two findings within one newer three-finding batch. Findings within either batch are not independent repetitions. The newer operational Python workflow is, however, an independent domain from the earlier Personal Git/PR workflow. Each of `ext-crl-2026-07-d/C02`, the single runtime-migration workflow `ext-crl-2026-07-e/C01`, and the single cross-worker guard workflow `ext-crl-2026-08-a/C01` is unknown-version supporting evidence rather than a known-version post-change observation. The `ext-crl-2026-08-c` cases are ten distinct workflows from one transferred source; each workflow is one evidence batch, and its planning, review, fixes, and verification do not increase the evidence count. Mapping one case to both CRL-X06 and CRL-X07 also does not increase that count.
- Normalized parent pattern:
  - A. Preflight ordering: preflight-detectable completion blockers are checked before the first mutation. Rejection at that stage leaves relevant state unchanged. This is supported by the earlier Git/PR case.
  - B. Per-boundary consistency: each independently mutable boundary has an explicit concurrency and consistency contract with an appropriate guard. Protection at one boundary does not protect another. This is now supported across the earlier Git/PR ref boundary and the newer operational durable-writer boundary.
  - C. Post-effect outcome truthfulness: after a visible or irreversible effect, status, retryability, and recovery direction reflect the actual resulting state. Whole-operation retry is claimed safe only when replay is demonstrably safe, for example through idempotency, deduplication, or an equivalent replay guard. Otherwise, the result directs reconciliation, forward recovery, compensation, or accurate partial/completed-with-warning handling instead of retrying the whole operation. The newer operational case is the first natural external evidence for this concern.
- Interpretation boundary: preflight-rejection no-op behavior does not imply universal workflow atomicity. Post-effect handling may accurately report completion with a follow-up warning, partial completion, required reconciliation, required forward recovery, or whole-operation retryability backed by explicit replay-safety guarantees. A reconciliation or recovery contract provides an alternative state-aware path; it does not by itself make replay of the original operation safe. Rollback, compensation, forward recovery, reconciliation, idempotency, deduplication, and replay guards are possible system-specific mechanisms, not universal implementation requirements; the generic requirement is truthful outcome, retry, and recovery-direction semantics for the resulting state.
- Related sub-issues: the earlier batch observed preflight ordering and per-boundary expected-old-state protection in its source workflow. In the newer batch, `C01` observed inaccurate whole-operation retryability after a visible effect, while `C03` observed incomplete adoption of a canonical write-safety boundary across actual writers. The latter is primarily per-boundary consistency and is only adjacent to repository-wide writer and entrypoint discovery; it is not `CRL-X02` post-change effectiveness evidence. In `ext-crl-2026-07-d/C02`, portable package validation was not modeled as a pre-mutation blocker. This naturally supports A. Preflight ordering and the generic requirement that portable package preflight validate the complete trust and mutation boundary before runtime changes, without creating a symlink-specific checklist. Strict rejection tests strengthen the local fix evidence, but operational transfer and target apply were not verified. In `ext-crl-2026-07-e/C01`, review found a background path that could write before a preflight-detectable completion blocker, supporting A, and response-unknown cleanup required an uncertain-write reconciliation direction, supporting C. The latter does not make whole-operation replay safe; replay still requires an independent replay-safety guarantee. In `ext-crl-2026-08-a/C01`, plan and implementation review found that a cross-worker pre-mutation guard depended on a protective marker without a sufficiently explicit read source, freshness, time-bound, failure-semantics, or visibility contract. This naturally supports A and B: state used for a cross-worker preflight guard must come from a control boundary with adequate freshness and visibility guarantees, and a write or protection at an audit/event, coordination-marker, or worker-local-cache boundary does not establish immediate safe observation at another boundary. An asynchronous audit or event path not designed and verified for the required read-after-write and visibility contract cannot be presumed to be the coordination authority. The case does not support C because it reports no post-effect status, whole-operation retry, or recovery-direction problem. A final check and the external action remain separated by a race, so the reported recheck does not establish workflow-wide atomicity.
- New supporting mapping: `ext-crl-2026-08-c/C02` supports B through invocation-local snapshot lifetime and live-control drift; `C04` supports A/B through readiness, consumption, invalidation, completeness, and authority boundaries; `C05` supports B/C through recovery identity, immutable reconstruction, read-only visibility, exports, and interval semantics; `C07` supports A/B/C through runnable rollback preflight, serialized mutation, schedule readback, and truthful completion; `C08` supports A/B/C through local-before-network gates, exact identity, single claim ownership, and ambiguous-effect reconciliation; `C09` supports B's trigger boundary by requiring an inventory of actual producers, timing, and shared effects before inferring concurrency; `C10` supports B/C through exact delivery identity, partial results, retry, and bounded continuation; `C11` supports A/B/C through disabled-effect gates, separate receipt/composition/claim boundaries, and ambiguous persistence; `C12` supports B because internally consistent artifacts can still drift across schema, derived values, and identity bindings; and `C14` supports A/B/C through function-level authority, authenticated durable evidence, and crash-prefix behavior. These are source-narrative mappings, not generic cache, rollout, delivery, pagination, artifact, path, or authority rules.
- Root-cause hypothesis: the packets did not make preflight ordering, preflight-rejection no-op behavior, per-boundary consistency, post-effect outcome and retry semantics, or discovery of all live mutation writers explicit. Independent implementation/final-diff review detected the earlier observed gaps, and independent multi-round plan review detected the new runtime-migration gaps. Confidence: medium; evidence spans multiple domains, while findings within each batch remain correlated and loaded skill versions are unknown.
- Prevention and detection: packet timing was present in the applicable batches; pre-implementation packets existed and received review. Packet completeness was incomplete because it missed decision-relevant effect boundaries, post-effect semantics, compatibility evidence, all live writers, or a cross-worker guard's read and visibility contract. Independent review detected those gaps. For `ext-crl-2026-08-a/C01` and the `ext-crl-2026-08-c` cases, the transferred narratives report review and verification results, but the underlying packets, diffs, command records, test outputs, and live effects are unavailable for central audit. Implementation/regression prevention and final fix verification remain unestablished centrally. This is not a `CRL-X01` packet-first failure.
- Repository rule state: `present`; the canonical `coding-review-loop` packet reference now includes the accepted conditional mutation/recovery contract under `Task-Specific Contracts`. This means the rule exists in the canonical package; it does not establish behavioral effectiveness or real-task improvement.
- Source-task attribution: `loaded version unknown`; the note names the skill but does not record a reliable loaded source or version, and the task date does not establish one. This is not evidence that a known present rule was skipped.
- Decision/change: the accepted existing-section refinement is implemented in `skills/coding-review-loop/references/review-packet-shape.md` in `47f774f`. It records the conditional transition map, boundary inventory, outcome matrix, and behavior-proof cross-reference without prescribing a particular mechanism. `skills/coding-review-loop/SKILL.md` is unchanged.
- Later comparable evidence: independent natural external evidence from another domain exists for per-boundary consistency, and the same newer batch supplies the first natural external post-effect outcome-truthfulness evidence. `ext-crl-2026-07-e/C01` adds natural plan/review-level support for A and C, while `ext-crl-2026-08-a/C01` adds natural supporting evidence for A and B from a cross-worker guard workflow with a real failure opportunity. The `ext-crl-2026-08-c` workflows add natural supporting evidence across A/B/C, including implementation and interaction facts reported by the source. All loaded versions are unknown, and the central record does not include the new source's underlying packets, diffs, command records, test outputs, or live-effect evidence. There is no known-version post-change causal attribution or known-version post-implementation real-task evidence yet.
- Next decision: observe known-version natural real tasks, including an applicable multi-step state-changing task and a nearby low-risk task that correctly does not trigger. Check whether the packet exposes decision-relevant constraints before implementation and whether final review can trace the packet to all actual boundaries and outcomes. Do not start a synthetic pilot.
- Outcome: `awaiting evidence`; the canonical rule now exists, but behavioral effectiveness and real-task improvement have not been established.
- Boundaries: external evidence only; not Stage 0 evidence and not promoted to policy, ADR, casebook, or formal evaluation.

#### CRL-X07 - Target-context verification fidelity

- Sources: `ext-crl-2026-07-d/C01` and `C02`, `ext-crl-2026-07-e/C01`, plus `ext-crl-2026-08-c/C04`, `C06`, `C08`, `C10`, and `C11`; external privacy-preserving evidence. The first two are distinct task cases on different dates from one external project. The runtime-migration source is one dated planning/review workflow from an independent project and domain; all of its HTTP, RPC, write, schema, import-time, and scheduled-effect findings remain one evidence case rather than separate repetitions. The five `ext-crl-2026-08-c` cases are five workflows from one transferred source, each counted once even when the same case also maps to CRL-X06.
- Normalized parent pattern: a claimed compatibility or runtime-independence contract must be verified in the target context it promises. Current documentation, a development process with extra dependencies, an injected or session-specific test environment, or compatibility evidence generated entirely by the implementation under test is insufficient when the target version, target runtime environment, or external peer contract differs.
- Evidence-backed subpatterns:
  - A. Version fidelity: the source of truth and behavior checks match the exact compatibility floor, not only current documentation. When compatibility targets a historical or minimum release, verification identifies that release and uses exact-version source, schema, release documentation, changelog, or an explicit version matrix as the source of truth. No single source form or target-binary check is universally required.
  - B. Environment fidelity: the execution check reproduces the promised offline, portable, isolated, or deployment-independent environment and default entrypoint rather than inheriting deployment-loaded modules, runtime paths, configuration, or process state. An isolated subprocess, clean/default invocation, absent deployment-only dependencies, absent runtime directory, or contract-limited inputs may be appropriate; subprocess execution is not a universal CLI requirement.
  - C. Peer/protocol fidelity: when compatibility is determined by an existing peer, legacy wire contract, or external producer/consumer, at least part of the evidence comes from or independently represents that external contract instead of letting the implementation under test generate both sides. Legacy-originated fixtures, privacy-safe wire samples, golden schema/data, an independent contract implementation, or real peer interoperability may fit; no particular fixture format, real network call, or live peer is universally required.
- Source findings and verification limits: `ext-crl-2026-07-d/C01` used current protocol documentation as historical schema even though the minimum supported release had a narrower field set and different required/optional constraints. Exact-release source inspection and repository behavior tests were completed, but an exact target-client configuration check and live connectivity were not. `ext-crl-2026-07-d/C02` found an offline entrypoint whose import-time deployment settings and deployment runtime default were masked by in-process tests. Isolated subprocess export, strict package rejection tests, and repository suites were completed, but authenticated private transfer and target deployment apply were not. In `ext-crl-2026-07-e/C01`, multi-round plan review found that broad runtime-migration compatibility claims lacked historical-version, clean-environment, and peer-independent protocol evidence. Local dependency feasibility and package metadata probes occurred; the revised clean-build, fresh-process, legacy-originated protocol, production-like acceptance, compatibility, write/recovery, and deployment gates remained planned rather than executed.
- Related earlier evidence: `ext-crl-2026-07-c/C02` observed that custom conditional behavior lost adjacent protocol behavior. It is related support for the peer/protocol concern, not version-fidelity or environment-fidelity evidence and not another case in the new batch. An injected or session-specific runner also hid clean-entrypoint failure in that earlier external project context; it supports environment fidelity and existing clean/default verification guidance.
- New supporting mapping: `ext-crl-2026-08-c/C04` supports C through the distinction between generic process liveness, callback readiness, and a durable reply consumer; `C06` supports C through producer/importer normalization exposed by a real receipt interaction; `C08` supports C through disagreement over mandatory end-to-end evidence identity; `C10` supports C through exact peer success, skipped, idempotent, and renderer-boundary shapes; and `C11` supports B/C through an exact consumer export and the runtime identity of the observation channel. The source reports no completed implementation or live interoperability for `C04`, and central distillation did not rerun or inspect the original verification artifacts for any of these cases.
- Root-cause hypothesis: verification inherited the current development context or implementation-under-test instead of identifying the promised target version, runtime-independence boundary, and independent source for an external peer contract. Confidence: medium; evidence includes an independent project and runtime-migration domain. That runtime-migration case and `ext-crl-2026-08-c/C04` remain plan/review-level, while the other new source cases report implementation or interaction facts whose underlying artifacts were not available for central verification.
- Repository rule state: `present`; the canonical packet reference now includes the accepted conditional Target-context contract under `Task-Specific Contracts`. This means the rule exists in the canonical package; it does not establish behavioral effectiveness, verified compatibility, or real-task improvement.
- Source-task attribution: `loaded version unknown` for all cases. The notes do not record a loaded skill source or version; task dates, repository commit dates, and current runtime linkage cannot establish historical task versions.
- Existing-guidance support: the `ext-crl-2026-07-d` cases support the existing source-of-truth/proxy/fallback, schema/API/output, Runnable Acceptance / Entrypoints, and Verification Plan guidance while showing the A/B gaps. `ext-crl-2026-07-e/C01` supports those areas plus Test Double Boundary Fidelity while showing the narrower peer/protocol evidence-independence gap. Its proposed legacy-originated fixtures and fresh-process checks are not completed implementation verification.
- Decision/change: the accepted Target-context refinement is implemented in the canonical packet reference, `skills/coding-review-loop/references/review-packet-shape.md`, as a conditional three-field existing-section extension. See `docs/session-reports/2026-07-30-crl-x07-target-context-refinement-implementation.md` for the implementation trace. `skills/coding-review-loop/SKILL.md` is unchanged.
- Status: `implemented / awaiting evidence`; not an active learning bet, with no synthetic pilot or promotion.
- Later comparable evidence: none from a known loaded post-implementation version. Earlier independent-project/domain natural evidence in `ext-crl-2026-07-e/C01` remains plan/review-level with `loaded version unknown`. The `ext-crl-2026-08-c` workflows add natural unknown-version support for B/C; `C04` remains plan-only, while the others report implementation or interaction facts that were not centrally reverified. None establishes implementation or regression prevention.
- Outcome: `awaiting evidence`; the evidence does not establish behavioral effectiveness or a known-version comparable outcome.
- Next decision: observe known-version natural tasks for one correct trigger and one nearby correct non-trigger; do not start a synthetic pilot.

### Evaluation Candidate Watchlist

- Focused reuse scan before adding helpers, fakes, wrappers, adapters, serializers, collection getters, fallbacks, or test doubles:
  - Candidate behavior: Agent should search same-file and sibling modules/tests before adding or changing same-concern helpers, especially during review-fix work.
  - Missing before promotion: privacy-safe fixture, expected-pass rubric, expected-fail examples, and judge method.
- Source-of-truth/fallback semantics:
  - Candidate behavior: Agent should verify docs, config, runtime evidence, or existing source helpers before encoding fallback behavior or data-source selection as a tested contract.
  - Missing before promotion: fixture showing an incorrect fallback temptation, visible source-of-truth evidence, and pass/fail criteria.
- Action/report semantic liveness:
  - Candidate behavior: Agent should verify acceptance-critical action, report, and enum-like values have a producer, consumer, and behavior test before treating a contract as alive.
  - Missing before promotion: privacy-safe fixture with a structurally present but unreachable business value and clear pass/fail rubric.
- Config-driven report truthfulness:
  - Candidate behavior: Agent should separate config labels from validated behavior and require bounds or negative tests before report output implies actionable recommendations.
  - Missing before promotion: privacy-safe fixture with misleading config labels, expected validation failure, and judge method.
- Semantic-responsibility reuse scan:
  - Candidate behavior: Agent should search by helper responsibility, not only feature keywords, and prefer existing source-of-truth helpers plus caller-local state before adding a new helper.
  - Missing before promotion: privacy-safe fixture with an existing source helper, a tempting duplicate, and clear abstraction-value criteria.
- Test double boundary fidelity:
  - Candidate behavior: Agent should fake the lowest practical external IO boundary and keep fake call shapes faithful enough to catch repo-owned helper contract regressions.
  - Missing before promotion: privacy-safe fixture where a middle-layer mock hides a call-signature regression.
- Dataflow contract preservation:
  - Candidate behavior: Agent should trace time-windowed, streaming, paginated, batched, deduped, or source-filtered contracts through callers.
  - Missing before promotion: privacy-safe fixture where helper shape is correct but caller widens, materializes, or recomputes from the wrong source.

## Evidence Boundary

- The Stage 0 tracker remains frozen.
- External private evidence can guide scope but does not become internal accepted dogfood.
- E008 requires a privacy-safe, judgeable, reproducible fixture.

## Open Questions

- Which checklist items are low-noise enough for the skill?
- Whether sandboxed execution can be tested in the current environment.
- Which E005-E007 notes should become fixtures first.
